<Window x:Class="RemoteManager.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helpers="clr-namespace:RemoteManager.Helpers"
        Title="{x:Static helpers:L.Input_Rename}" Height="160" Width="380"
        Icon="pack://application:,,,/RemoteManager;component/Resources/Icons/app_icon.png"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BgBrush}"
        Foreground="{StaticResource FgBrush}">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding Message}" Margin="0,0,0,10" FontSize="14"/>
        <TextBox Grid.Row="1" x:Name="InputBox" Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,15" Padding="6,4"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{x:Static helpers:L.Input_Cancel}" Padding="15,8" Margin="0,0,8,0" Click="OnCancel"
                    Background="Transparent" BorderBrush="{StaticResource CtrlBorderBrush}"
                    BorderThickness="1"/>
            <Button Content="{x:Static helpers:L.Input_OK}" Padding="25,8" Click="OnOk"
                    Background="{StaticResource AccentBrush}" Foreground="White"
                    BorderThickness="0" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>
