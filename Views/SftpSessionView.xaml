<UserControl x:Class="RemoteManager.Views.SftpSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:helpers="clr-namespace:RemoteManager.Helpers"
             Background="{StaticResource BackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- SFTP Toolbar -->
        <Border Grid.Row="0" Background="{StaticResource SidebarBrush}"
                BorderBrush="{StaticResource CtrlBorderBrush}"
                BorderThickness="0,0,0,1" Padding="8,4">
            <StackPanel Orientation="Horizontal">
                <Button Content="{x:Static helpers:L.Sftp_Disconnect}" Margin="2,0" FontSize="11"
                        Command="{Binding DisconnectCommand}"
                        IsEnabled="{Binding IsConnected}"
                        Padding="8,4" Background="{StaticResource RedBrush}" Foreground="White"/>
                <Button Content="{x:Static helpers:L.Sftp_Reconnect}" Margin="8,0,2,0" FontSize="11"
                        Command="{Binding ConnectCommand}"
                        IsEnabled="{Binding IsConnecting, Converter={StaticResource InverseBool}}"
                        Padding="8,4" Background="{StaticResource AccentBrush}" Foreground="White"/>

                <Separator Width="1" Background="{StaticResource CtrlBorderBrush}" Margin="8,4"/>
                
                <Button Content="{x:Static helpers:L.Sftp_Up}" Margin="2,0" FontSize="11"
                        Command="{Binding NavigateUpCommand}"
                        Padding="8,4" Background="Transparent" Foreground="{StaticResource FgBrush}" BorderThickness="0"/>
                
                <Button Content="{x:Static helpers:L.Sftp_Refresh}" Margin="2,0" FontSize="11"
                        Command="{Binding RefreshFilesCommand}"
                        Padding="8,4" Background="Transparent" Foreground="{StaticResource FgBrush}" BorderThickness="0"/>

                <Button Content="{x:Static helpers:L.Sftp_UploadFile}" Margin="8,0,2,0" FontSize="11"
                        Command="{Binding UploadFileCommand}"
                        Padding="8,4" Background="Transparent" Foreground="{StaticResource AccentBrush}" BorderThickness="0"/>

                <Separator Width="1" Background="{StaticResource CtrlBorderBrush}" Margin="8,4"/>

                <TextBlock Text="{Binding CurrentPath}" VerticalAlignment="Center" Margin="8,0"
                           FontSize="12" Foreground="{StaticResource FgBrush}"/>
                           
                <TextBlock Text="·" VerticalAlignment="Center" Margin="4,0" FontSize="11" Foreground="{StaticResource FgBrush}"/>
                <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center" Margin="4,0"
                           FontSize="11" Foreground="{StaticResource FgBrush}" Opacity="0.8"/>
            </StackPanel>
        </Border>

        <!-- Progress Bar -->
        <Border Grid.Row="1" Background="{StaticResource BackgroundBrush}"
                Visibility="{Binding IsTransferring, Converter={StaticResource BoolToVis}}">
            <ProgressBar Value="{Binding TransferProgress}" Maximum="100" Height="4" BorderThickness="0"
                         Foreground="{StaticResource AccentBrush}" Background="Transparent" />
        </Border>

        <!-- File List -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Files}" SelectedItem="{Binding SelectedFile}"
                  AutoGenerateColumns="False" HeadersVisibility="Column"
                  GridLinesVisibility="None" RowHeaderWidth="0"
                  Background="Transparent" RowBackground="Transparent"
                  Foreground="{StaticResource FgBrush}"
                  BorderThickness="0" IsReadOnly="True"
                  SelectionMode="Single" SelectionUnit="FullRow"
                  AllowDrop="True" Drop="DataGrid_Drop">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="{StaticResource SidebarBrush}"/>
                    <Setter Property="Foreground" Value="{StaticResource FgBrush}"/>
                    <Setter Property="Padding" Value="8,6"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="BorderBrush" Value="{StaticResource CtrlBorderBrush}"/>
                </Style>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="{StaticResource CtrlBorderBrush}"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource HoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{StaticResource SelectedBrush}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="4"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DataGrid.Resources>

            <DataGrid.InputBindings>
                <MouseBinding MouseAction="LeftDoubleClick" 
                              Command="{Binding NavigateToCommand}" 
                              CommandParameter="{Binding SelectedFile}" />
            </DataGrid.InputBindings>

            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="{x:Static helpers:L.Sftp_EnterDirectory}" Command="{Binding NavigateToCommand}" CommandParameter="{Binding SelectedFile}" />
                    <MenuItem Header="{x:Static helpers:L.Sftp_Download}" Command="{Binding DownloadFileCommand}" CommandParameter="{Binding SelectedFile}" />
                    <Separator/>
                    <MenuItem Header="{x:Static helpers:L.Sftp_Delete}" Command="{Binding DeleteItemCommand}" CommandParameter="{Binding SelectedFile}" Foreground="Red" />
                </ContextMenu>
            </DataGrid.ContextMenu>

            <DataGrid.Columns>
                <DataGridTemplateColumn Header="{x:Static helpers:L.Sftp_Name}" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="4,0">
                                <TextBlock Text="{Binding Icon}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="{x:Static helpers:L.Sftp_Size}" Binding="{Binding SizeString}" Width="100"/>
                <DataGridTextColumn Header="{x:Static helpers:L.Sftp_Modified}" Binding="{Binding LastWriteTime, StringFormat=\{0:yyyy-MM-dd HH:mm\}}" Width="140"/>
                <DataGridTextColumn Header="{x:Static helpers:L.Sftp_Permissions}" Binding="{Binding Permissions}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
